gost-backup-vault 🔐
🇷🇺 Русская версия | 🇬🇧 @README.md

CILicense: Apache-2.0Python 3.10+

Описание
gost-backup-vault — это CLI-обёртка над restic, borg и tar, добавляющая опциональное ГОСТ-шифрование (Магма, Кузнечик), YAML-политики и метрики Prometheus для задач бэкапа на Linux. Инструмент рассчитан на админов Linux, DevOps и специалистов по безопасности, которым нужны предсказуемые и аудируемые бэкапы с профилями российской криптографии. Политики валидируются, бэкапы запускаются через CLI и легко встраиваются в cron/systemd и стек наблюдаемости.

Основные возможности
Единая команда gost-backup с подкомандами init, backup, restore (прототип), check, metrics-server, policy-wizard.
YAML-политики с валидацией (Pydantic + проверка уникальных заданий, расписаний, путей).
Несколько backend-ов: restic, borg или tar с поддержкой дополнительных аргументов.
Опциональный слой ГОСТ-шифрования (Магма/Кузнечик) через подключаемых провайдеров; режим pass-through, если шифрование вынесено наружу.
Экспортёр Prometheus с метриками gost_backup_* (статус, длительность, объём, ошибки).
Интерактивный мастер для генерации конфигурации.
Подходит для cron/systemd таймеров; CLI-ориентированный подход с понятными кодами возврата.
Типизированный Python-проект с тестами, линтингом и CI.
Архитектура и компоненты
CLI на Typer → загрузчик конфига → валидатор политик → Executor → backend (restic | borg | tar).
Крипто-провайдер (по умолчанию local_software как заглушка) может оборачивать потоки для ГОСТ-шифрования или пропускать данные.
Маппер метрик обновляет gauge/counter; сервер метрик отдаёт /metrics через prometheus_client.
Хранилище зависит от backend: репозиторий restic/borg или директория с tar-архивами.
ASCII-схема:

[YAML config] -> [ConfigLoader + PolicyValidator] -> [Executor]
                    |-> [Backend: restic | borg | tar] -> [Repo/Storage]
                    |-> [Crypto provider]
                    |-> [MetricsMapper] -> [/metrics для Prometheus/Grafana]
Требования
Хост под Linux (POSIX; использует fcntl), CI проверяется на Ubuntu-latest.
Python 3.10+ с pip/venv.
Наличие в PATH нужных утилит: restic, borg, tar.
Доступ к исходным данным (часто нужен root/sudo) и к каталогу/репозиторию хранения.
При использовании реального ГОСТ-шифрования — внешний инструмент с поддержкой ГОСТ (например, OpenSSL с ГОСТ-движком или иной провайдер).
Сеть: исходящий доступ к удалённым репозиториям, входящий к порту 9105 для метрик (по необходимости).
Замечания и допущения
Секреты для репозиториев (RESTIC_PASSWORD, RESTIC_PASSWORD_FILE, BORG_PASSPHRASE) задаются через переменные окружения или секрет-хранилища.
Планировщик — внешний (cron/systemd/k8s); поле schedule в YAML носит описательный характер.
Заменяйте примерные пути/домены на свои (<YOUR_SERVER_IP>, <YOUR_STORAGE_PATH>).
Встроенный крипто-провайдер — софт-заглушка; для соответствия ГОСТ используйте полноценный плагин.
Быстрый старт
Клонирование и установка:
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install .
Создание минимальной политики:
cat <<'EOF' > gost-backup.yaml
node:
  id: node-1
  os: debian-12
backend:
  type: restic
  repo: /var/backups/restic-repo
  extra_args: []
crypto_profile:
  name: gost-magma
  gost_cipher: magma
  mode: wrap_key
  provider: local_software
jobs:
  - name: etc-and-home
    paths:
      - /etc
      - /home
    exclude:
      - /home/*/.cache
    schedule: "daily@02:00"
    retention:
      type: tiered
      daily: 7
      weekly: 4
      monthly: 6
EOF
Экспорт секрета backend (пример для restic):
export RESTIC_PASSWORD=<SET_STRONG_PASSWORD>
Инициализация репозитория и запуск задания:
gost-backup init --config gost-backup.yaml
gost-backup backup --config gost-backup.yaml --job etc-and-home
Публикация метрик:
gost-backup metrics-server --config gost-backup.yaml --listen 0.0.0.0:9105
# Сбор: curl http://<YOUR_SERVER_IP>:9105/metrics
Установка
Ubuntu / Debian
sudo apt update
sudo apt install -y restic borgbackup tar python3-venv python3-pip
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv /opt/gost-backup-venv
source /opt/gost-backup-venv/bin/activate
pip install -U pip
pip install .
RHEL / Rocky / Alma
sudo dnf install -y restic borgbackup tar python3-pip python3-virtualenv
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv /opt/gost-backup-venv
source /opt/gost-backup-venv/bin/activate
pip install -U pip
pip install .
Из исходников (editable для разработки)
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .[dev]
Настройка
Основной конфиг — YAML (схема в src/gost_backup_vault/config/schema.yaml). Ключевые поля:

node:
  id: node-1              # логический идентификатор
  os: debian-12           # опционально
backend:
  type: restic            # restic | borg | tar
  repo: /var/backups/restic-repo
  extra_args: ["--host", "node-1"]  # дополнительные флаги
crypto_profile:
  name: gost-magma
  gost_cipher: magma      # magma | kuznyechik | none
  mode: wrap_key          # wrap_key | full_encrypt
  provider: local_software
jobs:
  - name: etc-and-home
    paths: ["/etc", "/home"]
    exclude: ["/home/*/.cache"]
    schedule: "daily@02:00"          # метаданные для вашего планировщика
    retention:
      type: tiered
      daily: 7
      weekly: 4
      monthly: 6
Переменные окружения:

Restic: RESTIC_PASSWORD или RESTIC_PASSWORD_FILE, URL репозитория (локальный путь, s3:, rclone:).
Borg: BORG_PASSPHRASE или BORG_PASSCOMMAND.
Tar: нужен только доступ к директории назначения.
Интерактивный мастер: gost-backup policy-wizard --output gost-backup.yaml.

Использование
Проверка конфига без действий:
gost-backup check --config gost-backup.yaml --dry-run
Запуск конкретного задания:
gost-backup backup --config gost-backup.yaml --job etc-and-home
Инициализация репозитория:
gost-backup init --config gost-backup.yaml
Запуск экспортёра метрик:
gost-backup metrics-server --config gost-backup.yaml --listen 0.0.0.0:9105
Генерация новой политики:
gost-backup policy-wizard --output my-policy.yaml
Пример systemd таймера (замените <CONFIG_PATH>):
# /etc/systemd/system/gost-backup.service
[Unit]
Description=gost-backup-vault job
[Service]
Type=oneshot
Environment=RESTIC_PASSWORD=<SECRET>
ExecStart=/opt/gost-backup-venv/bin/gost-backup backup --config <CONFIG_PATH> --job etc-and-home

# /etc/systemd/system/gost-backup.timer
[Unit]
Description=Run gost-backup-vault daily
[Timer]
OnCalendar=*-*-* 02:00:00
Persistent=true
[Install]
WantedBy=timers.target
sudo systemctl daemon-reload
sudo systemctl enable --now gost-backup.timer
Команда restore пока заглушка; при необходимости используйте инструменты backend напрямую.
Обновление
Обновить код и переустановить:
git pull
pip install -U .
Повторно проверить конфиг:
gost-backup check --config gost-backup.yaml --dry-run
Перезапустить таймеры/сервисы после обновлений.
Логи и устранение неполадок
Логи идут в stdout/stderr; ориентируйтесь на код возврата.
При запуске через systemd: journalctl -u gost-backup.service -f.
Метрики: curl http://localhost:9105/metrics и проверка gost_backup_*.
Частые проблемы:
Нет утилиты backend: установите restic/borg/tar и добавьте в PATH.
Репозиторий не инициализирован или пароль не совпадает: выполните gost-backup init, проверьте RESTIC_PASSWORD/BORG_PASSPHRASE.
Нет прав на исходные пути: запускайте под root или выдайте доступ.
Порт занят: измените --listen на другой адрес/порт.
Замечания по безопасности
Используйте сильные пароли и храните их вне конфигов.
Ограничьте доступ к метрикам и репозиторию через firewall/VPN/reverse proxy.
Не публикуйте репозитории и конфиги в Интернет; выставляйте строгие права на файлы (например, 600).
Запускайте бэкапы от отдельного сервисного пользователя с минимальными правами.
Проверяйте внешние крипто-провайдеры и бинарники, которые попадают в PATH.
Структура проекта
src/gost_backup_vault/     # CLI, backend-ы, загрузчик крипто-провайдеров, политика/метрики
scripts/                   # утилиты для lint/test/perf/security
tests/                     # unit- и интеграционные тесты
.github/workflows/         # CI и security pipelines
pyproject.toml             # конфигурация упаковки и инструментов
LICENSE                    # лицензия Apache-2.0
Планы
Полные сценарии восстановления и список снапшотов для всех backend-ов.
Реализация политики хранения (prune/forget) на уровне backend.
Хелперы для удалённых хранилищ (SSH/S3/Rclone) и секретов.
Дополнительные плагины крипто-провайдеров с сертифицированными ГОСТ-движками.
Пакетирование (deb/rpm), опциональные Docker/systemd unit-ы, расширенные метрики/дашборды.
Как внести вклад
Открывайте issue или feature request в GitHub.
Делайте форк и PR; держите изменения небольшими и документированными.
Перед PR запускайте проверки:
bash scripts/lint.sh
bash scripts/test.sh
Соблюдайте существующий стиль: тайпинги, Ruff/Mypy чистые, YAML с аккуратными отступами.
Лицензия
Apache License 2.0. См. LICENSE.

Автор и коммерческая поддержка
Автор: Ranas Mukminov.
Для внедрения в продакшене, настройки под вашу инфраструктуру и аудита безопасности вы можете обратиться за коммерческой поддержкой на сайте https://run-as-daemon.ru или через профиль GitHub.
