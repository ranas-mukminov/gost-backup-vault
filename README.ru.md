# gost-backup-vault 🔐

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

![CI](https://github.com/ranas-mukminov/gost-backup-vault/workflows/CI/badge.svg) ![License: Apache-2.0](https://img.shields.io/badge/License-Apache%202.0-blue.svg) ![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)

## Описание

**gost-backup-vault** — это CLI-обёртка на Python 3.10+ вокруг `restic`, `borg` и `tar`, которая добавляет опциональный слой шифрования по ГОСТ (Магма, Кузнечик), политики резервного копирования на основе YAML и метрики Prometheus для задач резервного копирования в Linux. Проект нацелен на системных администраторов Linux, DevOps-инженеров и команды безопасности, которым требуются предсказуемые, аудируемые и управляемые политиками решения для резервного копирования с поддержкой российских криптографических стандартов. Инструмент проверяет политики резервного копирования, выполняет задачи бэкапа и предоставляет метрики наблюдаемости для интеграции с cron/systemd-таймерами и стеками мониторинга, такими как Prometheus и Grafana.

## Основные возможности

- **Единая точка входа CLI** (`gost-backup`) с подкомандами: `init`, `backup`, `restore` (прототип), `check`, `metrics-server`, `policy-wizard`
- **Политики на основе YAML**, валидируемые схемой Pydantic с комплексными проверками (уникальность имён задач, корректность расписаний, существование путей)
- **Множественные бэкенды резервного копирования**: restic, borg или tar-архивы с опциональными дополнительными аргументами и поддержкой pass-through
- **Опциональный слой шифрования ГОСТ**: шифрование Магма/Кузнечик через подключаемые криптопровайдеры или режим pass-through для внешнего шифрования
- **Экспортер метрик Prometheus**, предоставляющий метрики `gost_backup_*` (статус задачи, длительность, размер резервной копии, счётчики сбоев) на настраиваемом порту
- **Интерактивный мастер создания политик** для быстрой генерации конфигурации с пошаговыми подсказками в CLI
- **Интеграция с cron и systemd-таймерами**: CLI-первый дизайн с чёткими exit-кодами для интеграции с планировщиками
- **Промышленное качество кода на Python**: полностью типизированный, протестированный pytest, проверенный Ruff/Mypy, CI/CD-процессы для контроля качества

## Архитектура и компоненты

Инструмент следует модульной конвейерной архитектуре:

```
[YAML-конфиг] → [ConfigLoader + PolicyValidator] → [Executor]
                    ├→ [Бэкенд: restic | borg | tar] → [Репозиторий/Хранилище]
                    ├→ [Crypto Provider: ГОСТ-обёртка или pass-through]
                    └→ [MetricsMapper] → [эндпоинт /metrics для Prometheus/Grafana]
```

**Компоненты**:

- **ConfigLoader**: парсит YAML-конфигурацию и валидирует её по схеме Pydantic
- **PolicyValidator**: проверяет уникальность имён задач, корректность расписаний, доступность путей, политики хранения
- **Executor**: оркестрирует рабочий процесс резервного копирования с выбранным бэкендом и криптопровайдером
- **Бэкенды**: подключаемые адаптеры для `restic`, `borg` или `tar` с настраиваемыми аргументами
- **Crypto Provider**: слой абстракции для шифрования по ГОСТ (Магма/Кузнечик) или прозрачный pass-through
- **MetricsMapper**: обновляет счётчики и gauges Prometheus; сервер метрик публикует эндпоинт `/metrics` через `prometheus_client`

**Хранилище**: зависит от выбранного бэкенда — репозиторий restic/borg (`file://`, `s3://`, `rclone:`) или директория tar-архивов.

## Требования

- **Операционная система**: Linux-хост (POSIX-совместимый; использует `fcntl`), протестировано на Ubuntu 22.04/24.04 в CI
- **Python**: Python 3.10+ с поддержкой pip и venv
- **Бинарные файлы бэкендов** (в зависимости от выбранного бэкенда):
  - `restic` (протестировано с 0.16+)
  - `borgbackup` (протестировано с 1.2+)
  - `tar` (GNU tar, обычно предустановлен)
- **Права доступа**: доступ к исходным путям резервного копирования (часто требуется `root` или `sudo`) и доступная для записи директория репозитория/хранилища
- **Опциональное криптооборудование**: OpenSSL с поддержкой ГОСТ или внешний плагин криптопровайдера для промышленного шифрования по ГОСТ
- **Сеть**:
  - Исходящий доступ к удалённым репозиториям при использовании облачных/удалённых бэкендов (S3, SFTP, rclone)
  - Входящий доступ к порту метрик (по умолчанию 9105), если метрики предоставляются Prometheus

### Замечания и допущения

- Учётные данные репозитория (`RESTIC_PASSWORD`, `RESTIC_PASSWORD_FILE`, `BORG_PASSPHRASE`) должны передаваться через переменные окружения или внешние хранилища секретов
- Планирование резервного копирования осуществляется внешними планировщиками (cron, systemd-таймеры, Kubernetes CronJobs); поле `schedule` в YAML является метаданными для документации
- Замените примеры-заполнители в примерах (`<YOUR_SERVER_IP>`, `<YOUR_STORAGE_PATH>`, `<YOUR_DOMAIN>`) на ваши реальные значения
- Встроенный криптопровайдер является программной заглушкой для разработки; используйте промышленный плагин ГОСТ-провайдера с сертифицированными средствами для соответствия требованиям

## Быстрый старт

**1. Клонирование и установка**:

```bash
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install .
```

**2. Создание минимального файла политики**:

```bash
cat <<'EOF' > gost-backup.yaml
node:
  id: node-1
  os: debian-12

backend:
  type: restic
  repo: /var/backups/restic-repo
  extra_args: []

crypto_profile:
  name: gost-magma
  gost_cipher: magma
  mode: wrap_key
  provider: local_software

jobs:
  - name: etc-and-home
    paths:
      - /etc
      - /home
    exclude:
      - /home/*/.cache
    schedule: "daily@02:00"
    retention:
      type: tiered
      daily: 7
      weekly: 4
      monthly: 6
EOF
```

**3. Экспорт учётных данных бэкенда** (пример для restic):

```bash
export RESTIC_PASSWORD=<УСТАНОВИТЕ_НАДЁЖНЫЙ_ПАРОЛЬ>
```

**4. Инициализация репозитория и запуск задачи резервного копирования**:

```bash
gost-backup init --config gost-backup.yaml
gost-backup backup --config gost-backup.yaml --job etc-and-home
```

**5. Предоставление метрик** (опционально):

```bash
gost-backup metrics-server --config gost-backup.yaml --listen 0.0.0.0:9105
# Получение метрик: curl http://<YOUR_SERVER_IP>:9105/metrics
```

## Установка

### Ubuntu / Debian

```bash
sudo apt update
sudo apt install -y restic borgbackup tar python3-venv python3-pip
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv /opt/gost-backup-venv
source /opt/gost-backup-venv/bin/activate
pip install -U pip
pip install .
```

### RHEL / Rocky / Alma Linux

```bash
sudo dnf install -y restic borgbackup tar python3-pip python3-virtualenv
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv /opt/gost-backup-venv
source /opt/gost-backup-venv/bin/activate
pip install -U pip
pip install .
```

### Из исходников (редактируемый режим для разработки)

```bash
git clone https://github.com/ranas-mukminov/gost-backup-vault.git
cd gost-backup-vault
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .[dev]   # устанавливает инструменты для линтинга и тестирования (pytest, ruff, mypy)
```

## Настройка

Основная конфигурация представлена в формате YAML (схема определена в `src/gost_backup_vault/config/schema.yaml`). Ключевые секции:

```yaml
node:
  id: node-1              # Логический идентификатор узла
  os: debian-12           # Необязательные метаданные ОС

backend:
  type: restic            # restic | borg | tar
  repo: /var/backups/restic-repo
  extra_args: ["--host", "node-1"]  # Опциональные флаги для бэкенда

crypto_profile:
  name: gost-magma
  gost_cipher: magma      # magma | kuznyechik | none
  mode: wrap_key          # wrap_key | full_encrypt
  provider: local_software

jobs:
  - name: etc-and-home
    paths: ["/etc", "/home"]
    exclude: ["/home/*/.cache"]
    schedule: "daily@02:00"          # Метаданные для планировщика
    retention:
      type: tiered
      daily: 7
      weekly: 4
      monthly: 6
```

**Переменные окружения**:

- **Restic**: `RESTIC_PASSWORD` или `RESTIC_PASSWORD_FILE`, URL репозитория (локальный путь, `s3://`, `rclone://`) по необходимости
- **Borg**: `BORG_PASSPHRASE` или `BORG_PASSCOMMAND`
- **Tar**: требуется только доступная для записи директория назначения

**Интерактивная генерация конфигурации**:

```bash
gost-backup policy-wizard --output gost-backup.yaml
```

## Использование и типичные задачи

**Валидация конфигурации без побочных эффектов**:

```bash
gost-backup check --config gost-backup.yaml --dry-run
```

**Запуск конкретной задачи резервного копирования**:

```bash
gost-backup backup --config gost-backup.yaml --job etc-and-home
```

**Инициализация репозитория бэкенда** (идемпотентная операция):

```bash
gost-backup init --config gost-backup.yaml
```

**Запуск экспортера метрик Prometheus**:

```bash
gost-backup metrics-server --config gost-backup.yaml --listen 0.0.0.0:9105
```

**Генерация новой политики**:

```bash
gost-backup policy-wizard --output my-policy.yaml
```

### Интеграция с systemd-таймерами

Пример systemd-сервиса и таймера (замените `<CONFIG_PATH>` и `<SECRET>`):

```ini
# /etc/systemd/system/gost-backup.service
[Unit]
Description=gost-backup-vault job

[Service]
Type=oneshot
Environment=RESTIC_PASSWORD=<SECRET>
ExecStart=/opt/gost-backup-venv/bin/gost-backup backup --config <CONFIG_PATH> --job etc-and-home

# /etc/systemd/system/gost-backup.timer
[Unit]
Description=Run gost-backup-vault daily

[Timer]
OnCalendar=*-*-* 02:00:00
Persistent=true

[Install]
WantedBy=timers.target
```

Включение и запуск таймера:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now gost-backup.timer
```

**Примечание**: команда восстановления (restore) в настоящее время является прототипом; проверяйте восстановление вручную с использованием нативных инструментов бэкенда (`restic restore`, `borg extract`, `tar xf`) по необходимости.

## Обновление

Получение последнего кода и переустановка:

```bash
cd gost-backup-vault
git pull
pip install -U .
```

Повторная валидация после обновления:

```bash
gost-backup check --config gost-backup.yaml --dry-run
```

Перезапуск systemd-таймеров/сервисов, если применимо:

```bash
sudo systemctl restart gost-backup.timer
```

## Логи, мониторинг и устранение неполадок

**Логи**:

- CLI логирует в stdout/stderr; проверяйте exit-коды для определения успеха/неудачи
- При запуске через systemd: `journalctl -u gost-backup.service -f`
- Логи, специфичные для бэкенда, через флаги подробного вывода restic или borg в `extra_args`

**Метрики**:

```bash
curl http://localhost:9105/metrics
```

Метрики включают `gost_backup_job_status`, `gost_backup_duration_seconds`, `gost_backup_size_bytes`, `gost_backup_failures_total`.

**Типичные проблемы**:

| Проблема | Решение |
|----------|---------|
| Отсутствует бинарный файл бэкенда | Убедитесь, что `restic`, `borg` или `tar` находится в `PATH` |
| Репозиторий не инициализирован или несовпадение паролей | Запустите `gost-backup init` и проверьте `RESTIC_PASSWORD`/`BORG_PASSPHRASE` |
| Отказ в доступе к исходным путям | Запускайте от имени `root` или предоставьте доступ на чтение учётной записи сервиса |
| Порт занят для метрик | Измените адрес/порт `--listen` (например, `--listen 0.0.0.0:9106`) |
| Ошибки валидации политики | Запустите `gost-backup check --dry-run` и исправьте синтаксис/логику YAML |

## Замечания по безопасности

- **Надёжные пароли**: используйте сгенерированные случайным образом надёжные пароли для репозиториев и периодически ротируйте их
- **Управление секретами**: храните пароли вне конфигурационных файлов (переменные окружения, менеджеры секретов, systemd credentials)
- **Контроль доступа**: ограничивайте доступ к метрикам и репозиторию через правила межсетевого экрана, VPN или аутентификацию reverse proxy
- **Права доступа к файлам**: устанавливайте строгие права на конфигурационные файлы (`chmod 600 gost-backup.yaml`)
- **Сервисные учётные записи**: запускайте задачи резервного копирования с выделенными сервисными учётными записями с минимальными привилегиями
- **Криптопровайдеры**: валидируйте и аудируйте любые внешние плагины криптопровайдеров перед использованием в продакшене
- **Не выставляйте** репозитории резервных копий или конфигурации в открытом виде в публичный Интернет

## Структура проекта

```
src/gost_backup_vault/     # CLI, бэкенды, загрузчик криптопровайдера, код политик/метрик
  ├── cli/                 # Команды Typer (backup, restore, init, check, metrics-server, policy-wizard)
  ├── backends/            # Реализации бэкендов restic, borg, tar
  ├── crypto/              # Абстракция криптопровайдера и локальная заглушка
  ├── config/              # YAML-схема и загрузчик конфигурации
  ├── policy/              # Валидатор и генератор политик
  ├── metrics/             # Маппер метрик Prometheus и сервер
  ├── domain/              # Доменные модели (Job, BackupResult и т.д.)
  └── runner/              # Оркестратор выполнения
scripts/                   # скрипты для линтинга, тестирования, проверки производительности и безопасности
tests/                     # юнит и интеграционные тесты (pytest)
.github/workflows/         # CI (lint, test) и сканирование безопасности (bandit, safety)
pyproject.toml             # конфигурация упаковки и инструментов
LICENSE                    # лицензия Apache-2.0
LEGAL.md                   # юридические дисклеймеры и замечания по соответствию
```

## Планы развития

- **Полноценные процессы восстановления**: листинг снепшотов, выборочное восстановление, кросс-бэкенд восстановление
- **Принудительное применение политик хранения**: автоматические операции prune/forget для каждого бэкенда с правилами, управляемыми политиками
- **Помощники для удалённого хранилища**: преднастроенные шаблоны для SSH, S3, Rclone-бэкендов с управлением учётными данными
- **Сертифицированные ГОСТ-провайдеры**: интеграция с сертифицированными криптографическими цепочками ГОСТ (аппаратные HSM, коммерческие СКЗИ)
- **Упаковка**: Debian/RPM-пакеты, Docker-образы, официальные systemd-юниты
- **Расширенные метрики и дашборды**: предварительно собранные Grafana-дашборды, правила алертинга, обнаружение аномалий

## Как внести вклад

- Открывайте issues или запросы на новые функции через [GitHub Issues](https://github.com/ranas-mukminov/gost-backup-vault/issues)
- Форкните репозиторий и отправляйте PR; изменения должны быть небольшими, сфокусированными и хорошо задокументированными
- **Запускайте проверки качества перед отправкой**:

  ```bash
  bash scripts/lint.sh
  bash scripts/test.sh
  ```

- **Стиль кода**: следуйте существующим соглашениям — аннотации типов везде, чистота по Ruff/Mypy, YAML с единообразными отступами
- **Тесты**: добавляйте юнит/интеграционные тесты для новых функций; поддерживайте покрытие тестами

## Лицензия

Apache License 2.0. См. [LICENSE](LICENSE).

## Автор и коммерческая поддержка

**Автор**: [Ranas Mukminov](https://github.com/ranas-mukminov)

Для внедрения в продакшене, аудита инфраструктуры, консалтинга по соответствию ГОСТ и постоянной поддержки посетите [https://run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь с автором через [профиль GitHub](https://github.com/ranas-mukminov).

Коммерческие услуги включают:

- Развёртывание и упрочнение в продакшене
- Интеграция пользовательских криптопровайдеров (HSM, сертифицированные СКЗИ)
- Разработка политик резервного копирования и аудиты соответствия
- Интеграция стека мониторинга (Prometheus/Grafana)
- DevOps/SRE-консалтинг для процессов резервного копирования
